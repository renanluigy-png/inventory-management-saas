# Desenvolvimento local — carregado automaticamente pelo `npm run dev` (vite).
# Vazio de propósito: o proxy do Vite (vite.config.ts) encaminha /api, /health
# e /uploads para http://localhost:3333, então as chamadas usam caminho relativo.
VITE_API_URL=
VITE_WS_URL=
